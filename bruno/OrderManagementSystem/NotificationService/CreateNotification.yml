info:
  name: CreateNotification
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7026/api/notifications
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
