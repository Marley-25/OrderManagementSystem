info:
  name: GetAll
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:5171/api/catalog/products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
